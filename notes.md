# Introduction

For two functions `f` and `g`, `(f ∘ g)(x) + f(g(x))`, which is pronounced as:
`f` "composed with" or "after" `g` equals to `f` of `g` of `x`.

Writing functions without mention of the arguments is called "point-free style".
